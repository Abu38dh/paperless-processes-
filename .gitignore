node_modules
# Keep environment variables out of version control
.env

/lib/generated/prisma

.vercel

.next

# Firebase service account key (sensitive - never commit!)
serviceAccountKey.json

# User uploaded files
public/uploads/

# Build info
tsconfig.tsbuildinfo

# Backup files
*.backup.tsx
*.backup.ts

# WhatsApp bot session & cache (large browser data)
whatsapp-bot/.wwebjs_auth/
whatsapp-bot/.wwebjs_cache/
